const express = require('express');
const router = express.Router();

router.post('/detect-key', async (req, res) => {
    // Placeholder for Phase 2
    res.json({ message: "Not implemented yet" });
});

module.exports = router;
